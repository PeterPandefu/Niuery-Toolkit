{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "screenshot", "longshot-panel"],
  "permissions": [
    "core:default",
    "core:event:allow-emit-to",
    "opener:default",
    "http:default",
    "global-shortcut:default",
    "autostart:default",
    "dialog:default"
  ]
}
